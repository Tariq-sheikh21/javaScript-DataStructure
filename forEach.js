// forEach Method in js
const numbers = [1,2,3,4,5,6];

numbers.forEach((number, index) =>{
    console.log(`${index}. ${number * 2}`);
})

//In this example, forEach iterates over each element in the numbers array and logs its double.